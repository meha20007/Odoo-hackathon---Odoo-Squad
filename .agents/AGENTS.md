# Project Ownership and Constraints Rules

## Team Ownership Mapping
* **Meha (You)**: `auth.py`, `vehicle.py`, `trip.py`, `rbac.py`
* **Supreet**: `driver.py`, `maintenance.py`, `fuel.py`, `reports.py`, models
* **Aditi**: `templates/`, `static/`, frontend
* **Kanvisha**: testing, `README.md`, bug tracking

## Constraints for Antigravity (Agentic Assistant)
1. **Scope of Editing**: Only edit or modify backend Python files owned by Meha (`auth.py`, `vehicle.py`, `trip.py`, `rbac.py`).
2. **Strict Exclusions**:
   - Never create or modify files owned by Supreet, Aditi, or Kanvisha.
   - Do not edit or create files in the `models` module/folder.
   - Do not modify any frontend files, including HTML (`templates/`), CSS, JavaScript (`static/`), or any static assets.
   - Do not modify test directories, the `README.md` file, or bug tracking logs.
3. **Task Alignment**: Ensure all future instructions are mapped to Meha's files only.
